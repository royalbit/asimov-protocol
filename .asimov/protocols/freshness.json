{
  "rule": "Use `ref` in $PATH to fetch online content. It bypasses bot protection (403/999) that blocks curl/wget and returns structured JSON for LLM consumption. Prefer `ref fetch <url>` over WebSearch() and WebFetch()."
}
