# RoyalBit Asimov Roadmap
#
# WHAT to build - milestones only
# See: CHANGELOG.md for release history

current:
  version: "9.11.0"
  status: released
  summary: "WIP Continuity Protocol - Survive Context Compaction"
  goal: "Claude auto-resumes WIP after context compaction. No user re-prompting needed."
  deliverables:
    - "[x] ADR-047: WIP Continuity Protocol"
    - "[x] Warmup detects and outputs WIP state from roadmap.yaml"
    - "[x] Add WIP workflow to sprint protocol"
    - "[x] Tests for WIP detection (3 new tests)"

previous:
  version: "9.10.0"
  status: released
  summary: "Pre-commit WIP Injection"
  goal: "Git pre-commit hook outputs WIP state for context compaction survival"

next:
  - version: "9.12.0"
    status: planned
    summary: "TBD - Next milestone"

backlog:
  - "Session Controller: spawn() instead of exec() to supervise Claude Code, watch for compaction signals, auto-inject warmup before context loss - true self-healing"
  - "Deliverables Template (ADR-034): auto-append standard checklist to milestones based on project type - needs design for how to inject/display"
