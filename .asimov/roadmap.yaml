# RoyalBit Asimov Roadmap
#
# WHAT to build - milestones only
# See: CHANGELOG.md for release history

current:
  version: "9.8.0"
  status: released
  summary: "Dependency Health Audit (STRICT)"
  goal: "GOOD CODE = Fresh deps, zero vulns, license-compliant. Pre-commit FAILS on violations."
  priority: NORMAL
  deliverables:
    - "[x] ADR-045: Dependency Health Protocol"
    - "[x] LICENSE file detection (MIT, Apache-2.0, GPL, BSD, Proprietary)"
    - "[x] License compatibility: auto-detect from LICENSE/Cargo.toml/package.json/pyproject.toml"
    - "[x] Security: FAIL commit on any vulnerability (cargo audit, pip-audit, npm audit, govulncheck)"
    - "[x] Freshness: FAIL commit on outdated deps (including MAJOR versions)"
    - "[x] Rust: cargo audit + cargo deny + cargo outdated"
    - "[x] Python: pip-audit + pip-licenses + pip list --outdated"
    - "[x] Node: npm audit + license-checker + npm outdated"
    - "[x] Go: govulncheck + go list -m -u all"
    - "[x] Flutter: dart pub outdated"
    - "[x] Pre-commit hooks: direct tool calls (no SPOF), FAIL on issues"
    - "[x] asimov doctor: full dependency health report with license display"
    - "[x] Tests passing (488 tests)"

previous:
  version: "9.7.0"
  status: released
  summary: "Dependency Setup Assistant"
  goal: "asimov init adds dev dependencies for coding standards tools"

next: []

backlog:
  - "Session Controller: spawn() instead of exec() to supervise Claude Code, watch for compaction signals, auto-inject warmup before context loss - true self-healing"
  - "Deliverables Template (ADR-034): auto-append standard checklist to milestones based on project type - needs design for how to inject/display"
