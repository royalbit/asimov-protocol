# RoyalBit Asimov Roadmap
#
# WHAT to build - milestones only
# See: CHANGELOG.md for release history

current:
  version: "7.7.0"
  status: released
  summary: "Multi-Platform Binary Releases (GitHub Actions)"
  goal: GREEN_CODING

next:
  - version: "7.8.0"
    summary: "Auto-Update Feature (asimov update)"
    goal: GREEN_CODING
    features:
      - "asimov update - Check for new version and self-update binary"
      - "asimov update --check - Just check, don't update"
      - "Version comparison against GitHub Releases API"
      - "Download correct platform binary automatically"
      - "The needs of the many: users stay current without manual intervention"

backlog:
  - "VS Code Extension: Syntax highlighting, validation (separate repo, TypeScript)"
  - "TOON Format Migration: Evaluate when Rust parser exists and ecosystem matures (token efficiency for LLMs)"

# ═══════════════════════════════════════════════════════════════════════════════
# KILLED (not building)
# ═══════════════════════════════════════════════════════════════════════════════
# MCP Server Mode - KILLED (ADR-029)
#   Reason: MCP interfaces cap at 30k-128k tokens. Asimov requires 200k.
#   Green coding: Don't build what won't work.
#   See: docs/adr/029-mcp-server-mode-killed.md

# ═══════════════════════════════════════════════════════════════════════════════
# BACKLOG NOTES (not validated)
# ═══════════════════════════════════════════════════════════════════════════════
# migrations.yaml details:
#   For migration projects (language migrations, framework upgrades, rewrites),
#   ensure the new code functions identically to the original.
#
#   Potential approaches:
#   - Test parity: Same test suite passes on old and new
#   - Contract testing: Define input/output contracts, verify both satisfy
#   - Property-based testing: Generate inputs, compare outputs
#   - Shadow mode: Run both in parallel, diff results
#   - Behavioral snapshots: Capture old behavior, assert new matches
#   - API contract verification: OpenAPI/gRPC schema compatibility
#   - Database migration validation: Data integrity checks
#
#   Key principle: "Migration complete" = "functionally equivalent" not just "compiles"
