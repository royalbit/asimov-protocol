# RoyalBit Asimov Roadmap
#
# WHAT to build - milestones only
# See: CHANGELOG.md for release history

current:
  version: "8.7.0"
  status: released
  summary: "asimov replay - Replay a session from git history"
  goal: Observability
  deliverables:
    - "[x] Add replay subcommand"
    - "[x] Show commits for today (default), yesterday, or custom range"
    - "[x] Show files changed, insertions, deletions"
    - "[x] Show velocity metrics"
    - "[x] Support -n, --yesterday, --since, -v options"
    - "[x] Tests pass (147 tests)"
    - "[x] Commit and push"
    - "[x] GitHub release: v8.7.0"
    - "[x] Installed to ~/bin"

next:
  - version: "8.8.0"
    summary: "Launcher mode + Project-type deliverables"
    goal: DevEx
    adr: "ADR-033, ADR-034"
    deliverables:
      # Launcher mode (ADR-033)
      - "[ ] asimov (no args) launches Claude Code"
      - "[ ] Use opus settings: MAX_THINKING_TOKENS=200000, --dangerously-skip-permissions, --model opus"
      - "[ ] Auto-prompt: 'run asimov warmup'"
      - "[ ] Detect inside/outside Claude Code"
      - "[ ] Error if claude not in PATH"
      # Project-type deliverables (ADR-034)
      - "[ ] Add .asimov/project.yaml schema"
      - "[ ] Add deliverables_template to coding templates (rust, python, node, go, flutter)"
      - "[ ] Non-coding templates (docs, generic) get simpler checklist"
      - "[ ] Update asimov init to generate project.yaml"
      # Standard (this is a coding project)
      - "[ ] Unit tests"
      - "[ ] E2E tests"
      - "[ ] Update README"
      - "[ ] Update --help"
      - "[ ] Commit, push, release"
      - "[ ] Install to ~/bin"

backlog:
  - "v9.0.0: Zig port (post Zig 1.0 release)"
