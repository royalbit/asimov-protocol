# RoyalBit Asimov Roadmap
#
# WHAT to build - milestones only
# See: CHANGELOG.md for release history

current:
  version: "8.16.0"
  status: in_progress
  summary: "Command Fixes from Validation"
  goal: Quality
  priority: HIGH
  deliverables:
    # COMPLETED
    - "[x] Remove: asimov check command + ADR-037"
    - "[x] Remove: asimov schema command + ADR-037"
    - "[x] Fix: asimov init - require --name and --type flags"
    - "[x] Fix: asimov warmup - add --verbose, simple output by default"
    - "[x] Fix: asimov doctor - auto-create .asimov/ and roadmap.yaml"
    - "[x] Fix: asimov validate - no args, validate ALL"
    - "[x] Fix: asimov refresh - regenerate .json protocol files"
    - "[x] Fix: asimov update - already correct (downloads from GH releases)"

    - "[x] Fix: asimov lint-docs - already works (built-in linter, scans ALL .md)"
    - "[x] Fix: asimov stats - show ENTIRE project stats + current session"

next:
  - version: "8.16.2"
    summary: "Protocol Content Review"
    goal: Quality
    priority: CRITICAL
    deliverables:
      - "[ ] Show user: asimov.json - user reviews and fixes"
      - "[ ] Show user: freshness.json - user reviews and fixes"
      - "[ ] Show user: sycophancy.json - user reviews and fixes"
      - "[ ] Show user: green.json - user reviews and fixes"
      - "[ ] Show user: sprint.json - user reviews and fixes"
      - "[ ] Show user: warmup.json - user reviews and fixes"
      - "[ ] Show user: migrations.json - user reviews and fixes"
      - "[ ] Show user: exhaustive.json - user reviews and fixes"

  - version: "8.17.0"
    summary: "Comprehensive E2E Test Suite"
    goal: Quality
    priority: HIGH
    deliverables:
      - "[ ] E2E tests for all validated behaviors"

previous:
  version: "8.15.1"
  status: released
  summary: "User Validation of ALL Command Behaviors"
  goal: Validation

backlog:
  - "Session Controller: spawn() instead of exec() to supervise Claude Code, watch for compaction signals, auto-inject warmup before context loss - true self-healing"
  - "Deliverables Template (ADR-034): auto-append standard checklist to milestones based on project type - needs design for how to inject/display"
