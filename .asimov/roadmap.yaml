# RoyalBit Asimov Roadmap
#
# WHAT to build - milestones only
# See: CHANGELOG.md for release history

current:
  version: "8.8.0"
  status: released
  summary: "Launcher mode + Project-type deliverables"
  goal: DevEx
  adr: "ADR-033, ADR-034"
  deliverables:
    # Launcher mode (ADR-033)
    - "[x] asimov (no args) launches Claude Code"
    - "[x] Use opus settings: MAX_THINKING_TOKENS=200000, --dangerously-skip-permissions, --model opus"
    - "[x] Auto-prompt: 'run asimov warmup'"
    - "[x] Detect inside/outside Claude Code"
    - "[x] Error if claude not in PATH"
    # Project-type deliverables (ADR-034)
    - "[x] Add deliverables_template to coding templates (rust, python, node, go, flutter)"
    - "[x] Non-coding templates (docs, generic) get simpler checklist"
    # Standard (this is a coding project)
    - "[x] Unit tests pass (147 tests)"
    - "[x] Update README"
    - "[x] Commit, push, release"
    - "[x] Install to ~/bin"

next: []

backlog: []
