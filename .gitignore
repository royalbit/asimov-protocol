# Build artifacts
/target

# Binaries (release via GitHub only)
forge-protocol-*
*.tar.gz
*.zip

# Generated presentations (regenerate with `make presentation`)
*.pdf
*.pptx

# OS files
.DS_Store

# Self-Healing Protocol checkpoint (session-specific, not committed)
.claude_checkpoint.yaml
