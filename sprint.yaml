# Sprint Autonomy Protocol
# WHEN to stop - Bounded sessions for sustainable AI development

# ═══════════════════════════════════════════════════════════════════════════════
# CORE RULES (Non-negotiable)
# ═══════════════════════════════════════════════════════════════════════════════
rules:
  max_hours: 4
  max_milestones: 1
  must_ship: true
  mantra: "Done is better than perfect. Ship it."

# ═══════════════════════════════════════════════════════════════════════════════
# CURRENT: v4.1.0 - HARDCODED ETHICS (ADR-011)
# ═══════════════════════════════════════════════════════════════════════════════
sprint:
  current: "v4.1.0 - Hardcoded Ethics"
  started: "2025-11-29"
  status: done

  # ─────────────────────────────────────────────────────────────────────────────
  # HARDCODED ETHICS (ADR-011)
  # ─────────────────────────────────────────────────────────────────────────────
  # Core ethics moved from removable ethics.yaml into CLI binary.
  # Raises bypass bar from "delete a file" to "rebuild the binary".
  # Makes ethics removal visible and intentional.
  # ─────────────────────────────────────────────────────────────────────────────

  tasks:
    - "[x] Create cli/src/ethics.rs with hardcoded CORE_PRINCIPLES"
    - "[x] Implement RED_FLAGS (27+ patterns, 4 categories)"
    - "[x] Add --ethics-scan flag for red flag detection"
    - "[x] Update validation output (HARDCODED/EXTENDED status)"
    - "[x] Update refresh command with ethics reminder"
    - "[x] Wire up ethics module in lib.rs"
    - "[x] Add 18 new unit tests"
    - "[x] Update CHANGELOG.md"
    - "[x] Bump version to 4.1.0"
    - "[x] Update roadmap.yaml"
    - "[x] Update ADR-011 status to Accepted"
    - "[x] Run tests and clippy (103 tests, 0 warnings)"

  deliverables:
    - "cli/src/ethics.rs - Core ethics module (300+ lines)"
    - "CORE_PRINCIPLES hardcoded (financial, physical, privacy, deception)"
    - "RED_FLAGS - 27 patterns across 4 categories"
    - "--ethics-scan CLI flag"
    - "Ethics status in validation output"
    - "Enhanced refresh command"

# ═══════════════════════════════════════════════════════════════════════════════
# PREVIOUS SPRINT (completed)
# ═══════════════════════════════════════════════════════════════════════════════
previous:
  sprint: "v4.0.0 - Claude Code Native Integration"
  status: done
  date: "2025-11-28"
  highlights:
    - "Strategic pivot to integrate with Claude Code 2.0"
    - "CLAUDE.md @import syntax"
    - "Deprecated cross-session features for native alternatives"

# ═══════════════════════════════════════════════════════════════════════════════
# SESSION PHASES
# ═══════════════════════════════════════════════════════════════════════════════
phases:
  1_define:
    duration: "5-10 min"
    actions:
      - "State ONE milestone"
      - "Define 'done'"
      - "Set time budget (max 4h)"

  2_execute:
    duration: "2-4 hours"
    rules:
      - "NO scope creep ('Let me also...')"
      - "NO tangents ('While I'm here...')"
      - "NO gold plating ('This would be better if...')"
    checkpoint: "Every 2 hours - Am I still on track?"

  3_ship:
    duration: "15-30 min"
    checklist:
      - "cargo test (all pass)"
      - "cargo clippy (zero warnings)"
      - "Update CHANGELOG.md"
      - "Commit, tag, push"
      - "cargo publish (if releasing)"

  4_stop:
    rule: "MANDATORY stop - no 'just one more thing'"

# ═══════════════════════════════════════════════════════════════════════════════
# ANTI-PATTERNS
# ═══════════════════════════════════════════════════════════════════════════════
anti_patterns:
  scope_creep: "Note it for NEXT session, don't do it now"
  perfectionism: "Working code > Perfect code. Ship it."
  rabbit_holes: "Interesting? Note it. Back to milestone."
  over_engineering: "Build what's needed NOW, not hypothetical futures"

# ═══════════════════════════════════════════════════════════════════════════════
# AI AUTHORITY
# ═══════════════════════════════════════════════════════════════════════════════
authority:
  principle: "Make decisions. Don't ask. Ship."
  can_release_when:
    - "All tests pass"
    - "Zero warnings"
    - "Within milestone scope"
  ask_human_only:
    - "Blocked by external dependency"
    - "Fundamental requirement ambiguity"
